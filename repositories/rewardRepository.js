/*
pointRepository.js
──────────────────

Functions needed:

1. create(transactionData)
   → Save new transaction
   → Return transaction

2. findByPupilId(pupilId)
   → Get all transactions for a pupil
   → Sort by newest first
   → Populate givenBy (teacher name)

3. findByClassroomId(classroomId)
   → Get all transactions in classroom
   → Sort by newest first
   → Populate pupilId and givenBy
*/

const rewardRepository = {
    
}